(function() {
    'use strict';

    angular.module('<%= moduleName %>')
        .controller('<%= controllerName %>', ['$scope',
            function <%= controllerName %>($scope) {

            }
        ]);

}());
